'use strict';

/**
 * template-undangan controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::template-undangan.template-undangan');
