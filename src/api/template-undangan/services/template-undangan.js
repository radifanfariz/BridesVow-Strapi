'use strict';

/**
 * template-undangan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::template-undangan.template-undangan');
